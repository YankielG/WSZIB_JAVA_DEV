#xxxxxxxxxxxxxx  settings posagre sql xxxxxxxxxxxxxx
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5
spring.datasource.url=jdbc:postgresql:${postgresql.address}:${postgresql.port}/${postgresql.name}
spring.datasource.username=${postgresql.user}
spring.datasource.password=${postgresql.pass}
spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
spring.jpa.hibernate.ddl-auto=update

#xxxxxxxxxxxxxx  settings posagre sql HEROKU xxxxxxxxxxxxxx
#spring.datasource.hikari.connectionTimeout=20000
#spring.datasource.hikari.maximumPoolSize=5
#spring.datasource.url=${JDBC_DATABASE_URL}
#spring.datasource.username=${JDBC_DATABASE_USERNAME}
#spring.datasource.password=${JDBC_DATABASE_PASSWORD}
#spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQL9Dialect
#spring.jpa.hibernate.ddl-auto=update
#create, create-drop, validate, update, none
